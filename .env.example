# Mapbox GL JS token for the travel calculator map
# In production, set this as a Cloudflare Pages environment variable
# Restrict the token to roadtoonu.com in the Mapbox dashboard
PUBLIC_MAPBOX_TOKEN=your_mapbox_token_here
