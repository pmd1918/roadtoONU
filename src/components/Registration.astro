---
import PreregisterButton from "./PreregisterButton";
---

<section class="registration-section" id="registration">
  <div class="registration-container">
    <div class="registration-content">
      <h2 class="registration-title">
        Secure Your Spot
      </h2>
      
      <p class="registration-intro">
        Join us for the 62nd National Conclave at Ohio Northern University—
        where we're not just gathering, we're celebrating a century of brotherhood.
      </p>
      
      <div class="registration-highlights">
        <div class="highlight-item">
          <img src="/icons/brotherhood.webp" alt="Brotherhood" class="highlight-icon-img" loading="lazy" />
          <h3 class="highlight-title">Brotherhood Reunion</h3>
          <p class="highlight-text">
            Reconnect with brothers from across the nation and forge new bonds 
            that will last a lifetime.
          </p>
        </div>
        
        <div class="highlight-item">
          <img src="/icons/centennial.webp" alt="Centennial" class="highlight-icon-img" loading="lazy" />
          <h3 class="highlight-title">Historic Milestone</h3>
          <p class="highlight-text">
            Be part of Mu Beta chapter's 100-year celebration at Ohio Northern 
            University—a once-in-a-lifetime event.
          </p>
        </div>
        
        <div class="highlight-item">
          <img src="/icons/football.webp" alt="Football" class="highlight-icon-img" loading="lazy" />
          <h3 class="highlight-title">Wilson Factory Tour</h3>
          <p class="highlight-text">
            Go behind the scenes at the world-famous Wilson Football Factory—where 
            every NFL game ball is handcrafted.
          </p>
        </div>
        
        <div class="highlight-item">
          <img src="/icons/network.webp" alt="Network" class="highlight-icon-img" loading="lazy" />
          <h3 class="highlight-title">Network & Grow</h3>
          <p class="highlight-text">
            Connect with alumni leaders, attend workshops, and build professional 
            relationships that extend beyond the chapter.
          </p>
        </div>
      </div>
      
      <div class="registration-cta">
        <PreregisterButton client:visible />
      </div>
    </div>
    
    <div class="lanyard-wrapper">
      <picture>
        <source media="(max-width: 768px)" srcset="/images/lanyard-badges-mobile.webp" />
        <img 
          src="/images/lanyard-badges-desktop.webp" 
          alt="Conclave lanyard with badges" 
          class="lanyard-image"
          loading="lazy"
          width="600"
          height="800"
        />
      </picture>
    </div>
  </div>
</section>

<style>
  .registration-section {
    position: relative;
    width: 100%;
    min-height: 100vh;
    background: #212322;
    padding: 2rem 2rem 0 2rem;
    overflow: visible;
  }
  
  @media (max-width: 768px) {
    .registration-section {
      padding: 1.5rem 1.5rem 0 1.5rem;
    }
  }
  
  .registration-container {
    max-width: 1400px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }
  
  @media (max-width: 1024px) {
    .registration-container {
      grid-template-columns: 1fr;
      gap: 0;
    }
    
    .lanyard-wrapper {
      order: -1;
    }
  }
  
  .registration-content {
    z-index: 10;
    padding: 2rem;
  }
  
  @media (max-width: 768px) {
    .registration-content {
      padding: 1rem;
    }
  }
  
  .registration-title {
    font-family: 'Have Heart', cursive;
    font-size: clamp(2.5rem, 6vw, 5rem);
    background: linear-gradient(135deg, #ea7600, #f1d44b);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    margin-bottom: 1.5rem;
    line-height: 1.3;
    padding-bottom: 0.3em;
    padding-left: 0.3em;
    overflow: visible;
  }
  
  .registration-intro {
    font-family: 'Inter', sans-serif;
    font-size: clamp(0.875rem, 1.5vw, 1rem);
    font-weight: 400;
    color: #f4f7ff;
    line-height: 1.6;
    margin-bottom: 3rem;
  }
  
  @media (max-width: 768px) {
    .registration-intro {
      margin-bottom: 2rem;
    }
  }
  
  .registration-highlights {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: clamp(0.75rem, 2vw, 2rem);
    margin-bottom: clamp(1.5rem, 3vw, 3rem);
  }
  
  .highlight-item {
    display: flex;
    flex-direction: column;
    gap: clamp(0.25rem, 0.75vw, 0.75rem);
  }
  
  .highlight-icon {
    font-size: clamp(1.5rem, 2.5vw, 2.5rem);
  }
  
  .highlight-icon-img {
    width: clamp(20px, 3vw, 32px);
    height: clamp(20px, 3vw, 32px);
    object-fit: contain;
  }
  
  .highlight-title {
    font-family: 'Inter', sans-serif;
    font-size: clamp(0.7rem, 1.5vw, 1.25rem);
    font-weight: 700;
    color: #f1d44b;
    margin: 0;
  }
  
  .highlight-text {
    font-family: 'Inter', sans-serif;
    font-size: clamp(0.6rem, 1.2vw, 1rem);
    font-weight: 400;
    color: #e8ecf4;
    line-height: 1.5;
    margin: 0;
  }
  
  .registration-cta {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: flex-start;
  }
  
  @media (max-width: 768px) {
    .registration-cta {
      align-items: stretch;
    }
  }
  
  .cta-button {
    display: inline-block;
    padding: 0.75rem 1.75rem;
    font-family: 'Inter', sans-serif;
    font-size: 0.9rem;
    font-weight: 700;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    border-radius: 50px;
    transition: all 0.3s ease;
    border: 2px solid transparent;
    text-align: center;
  }
  
  @media (max-width: 768px) {
    .cta-button {
      padding: 0.6rem 1.5rem;
      font-size: 0.8rem;
    }
  }
  
  .cta-button.primary {
    background: linear-gradient(135deg, #ea7600, #f1d44b);
    color: #212322;
    box-shadow: 0 4px 20px rgba(234, 118, 0, 0.4);
  }
  
  .cta-button.primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 32px rgba(234, 118, 0, 0.6);
  }
  
  .registration-note {
    font-family: 'Inter', sans-serif;
    font-size: clamp(0.875rem, 1.5vw, 1rem);
    font-weight: 400;
    color: #8a8e96;
    margin: 0;
    font-style: italic;
  }
  
  .lanyard-wrapper {
    position: relative;
    z-index: 0;
    width: 100%;
    height: clamp(280px, 50vw, 600px);
    display: flex;
    justify-content: center;
    align-items: center;
  }
  
  .lanyard-image {
    max-width: 100%;
    max-height: 100%;
    width: auto;
    height: auto;
    object-fit: contain;
  }
</style>
